<Page
    x:Class="Player.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Player"
    xmlns:c="using:Player.Converter"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Page.Resources>
        <c:TimeConverter x:Key="converter"/>
    </Page.Resources>

    <Grid>
        <Grid.Background>
            <ImageBrush ImageSource="Assets/back.jpg" Opacity="0.5"/>
        </Grid.Background>
        <Grid Height="400" Width="500" HorizontalAlignment="Center" VerticalAlignment="Center">
            <Grid.RowDefinitions>
                <RowDefinition Height="320"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <Ellipse x:ConnectionId='1' x:Name="picture" Height="300" Width="300" Grid.Row="0" Visibility="Collapsed" RenderTransformOrigin="0.5,0.5" VerticalAlignment="Center" HorizontalAlignment="Center">
                <Ellipse.RenderTransform>
                    <CompositeTransform />
                </Ellipse.RenderTransform>

                <Ellipse.Resources>
                    <!--Storyboard是一个动画容器-->
                    <Storyboard x:ConnectionId='4' x:Name="EllStoryboard" RepeatBehavior="Forever">
                        <DoubleAnimation Duration="0:0:20" To="360" Storyboard.TargetProperty="(UIElement.RenderTransform).(CompositeTransform.Rotation)" Storyboard.TargetName="picture" d:IsOptimized="True"/>
                    </Storyboard>
                </Ellipse.Resources>

                <Ellipse.Fill>
                    <ImageBrush ImageSource="Assets/music.jpg" />
                </Ellipse.Fill>
            </Ellipse>

            <MediaElement x:ConnectionId='2' Source="ms-appx:///Assets/week4.mp4" Grid.Row="0" x:Name="mediaPlayer" Width="400" AutoPlay="True"                           AreTransportControlsEnabled="False"/>

            <Slider x:ConnectionId='3' Grid.Row="1" Padding="50,0,50,0" Value="{Binding ElementName=mediaPlayer, Path=Position, Converter={StaticResource converter}, Mode=TwoWay}" x:Name="time" />
        </Grid>

    </Grid>

    <Page.BottomAppBar>
        <CommandBar>
            <AppBarButton x:ConnectionId='5' Name="start" Icon="Play" FontSize="20"                      Label="播放"/>
            <AppBarButton x:ConnectionId='6' Name="pause" Icon="Pause" FontSize="20"                      Label="暂停"/>
            <AppBarButton x:ConnectionId='7' Name="stop" Icon="Stop" FontSize="20"                     Label="停止"/>
            <AppBarButton x:ConnectionId='8' Name="full" Icon="FullScreen" FontSize="20"                     Label="全屏"/>
            <AppBarButton x:ConnectionId='9' Name="chose" Icon="OpenFile" FontSize="20"                      Label="选择文件"/>
        </CommandBar>
    </Page.BottomAppBar>
</Page>

